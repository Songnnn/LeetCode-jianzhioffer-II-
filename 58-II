class Solution {
public:
    string reverseLeftWords(string s, int n) {
        string str1 = "";
        string str2 = "";
        for (int i = 0; i < s.size(); i++) {
            if (i < n) str1 += s[i];
            else str2 += s[i];
        }
        return str2 + str1;
    }
};
